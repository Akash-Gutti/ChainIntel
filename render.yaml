services:
  - type: web
    name: chainintel
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app/app.py
    autoDeploy: true
    envVars:
      - key: PORT
        value: 7860
